apiVersion: v1
kind: Service
metadata:
  name: {{ include "mempass.fullname" . }}
  labels:
    {{- include "mempass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mempass.selectorLabels" . | nindent 4 }}
